AD Tidbits
# Provder
# Don't load AD Drive - Cmdlets Only
$env:adps_loaddefaultdrive = 0
# Load AD Drive with easier to use syntax
Remove-PSDrive AD
New-PSDrive -Name AD -Root "" -PSProvider ActiveDirectory -FormatType Canonical
# Demo customized Get-ADUser

Use Search-ADAccount for accountDisabled, inactive, expiring, lockedout, passwd expired





Create a Repository - Using Windows Tools from GitHub.com - Initially - do not push to GitHub.com
Create file(s) in repo that you want to track (Demonstrate-ChangeControl.ps1 and demofile.txt)
Use Add-GitFile function 

md demo
cd demo
"foo" > foo.txt
"bar" > bar.txt
# initialize the repository
git init
notice prompt "current branch" | # files that have been edited since commit | 
git add *
git commit *
git tag -a V1.0
git log --decorate --oneline
git branch BasedOnV.1Tag v1.0

#Demo AD Stuff

#Provider - describe providers generically - 1 -2 minutes

$env:adps_loaddefaultdrive = 0
 #get rid of existing default drive
Remove-PSDrive AD

New-PSDrive -Name AD -Root "" -PSProvider ActiveDirectory -FormatType Canonical
# Mess around with ls and dir using wildcards the nicer way

# Demo Get-Process vs Get-ADUser
get-process 
get-process win*
get-process svc*

# Demo Get-Aduser
Get-Aduser Administrator # So far so good
#prompts for filter param - can take * 
Get-Aduser 
# what about finer fiilter
get-aduser -filter admin*
get-aduser -filter 'name -like "Admin*"' 

# So the out of the box Cmdlet is broken - whats a PS guy to do
# we fix it using Proxy Functions

# Demo New-ProxyFunction



# GIT
#create an alias
git config --global alias.andylog "log --oneline --decorate"
git branch --m oldbranchname newbranchname # Rename a branch
git reflog # shows everything.. how to undelete - create a branch to specif sha 
